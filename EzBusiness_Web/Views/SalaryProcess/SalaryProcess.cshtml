
@{
    ViewBag.Title = "SalaryProcess";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="~/Scripts/EzComNDW.js"></script>
<script>
    $(function () {

        EzAuthentication("/SalaryProcess");

        EzdtePkMMyy('#DateINMonth');

        var smaHtml = $("#SMAContainer").html();
        $("#SMAListContainer1").hide();
        function GetSlry() {
            $.get("/SalaryProcess/GetSalaryDetails", function (response) {
                $("#SMAListContainer").html(response);
            });
        }
        $("#btnNew").click(function () {
            //
            var n = EzAuthenticationBtn("/SalaryProcess", "NewIt");
            if (n == 1) {
                $.get("SalaryProcess/AddSalesProcess", function (response) {
                    $("#SMAContainer").html(response);
                });

                $(this).prop("disabled", true);
                EzbtnNewAc();
                $("#SMAListContainer1").hide();
                $("#editshow").hide();
            }
        });

        $(".alert-close").click(function () {
            $("#editshow").hide();
        });
       
        $("#btnEdit").click(function () {
            var n = EzAuthenticationBtn("/SalaryProcess", "ViewIt");
            if (n == 1) {
                $(this).prop("disabled", true);
                $("#btnNew").prop("disabled", true);
                $("#SMAContainer").html();
                $("#hdnOperationMode").val("Edit");
                $("#btnSave").prop("disabled", false);
                $("#SMAListContainer1").show();
                GetSlry();
                $("#editshow").show();
            }
        });

        $("#btnCancel").click(function () {
            $("#SMAContainer").html(smaHtml);
            $("#btnNew").prop("disabled", false);
            $("#btnEdit").prop("disabled", false);
            $("#btnDelete").prop("disabled", true);
            $("#hdnOperationMode").val("");
            $("#btnSave").prop("disabled", true);
            $("#SMAListContainer1").hide();
            $("#editshow").hide();
        });

        $("#btnDelete").click(function () {
            var n = EzAuthenticationBtn("/SalaryProcess", "DeleteIt");
            if (n == 1) {
                $("#editshow").hide();
                debugger;
                    const swalWithBootstrapButtons = Swal.mixin({
                        confirmButtonClass: 'btn btn-success',
                        cancelButtonClass: 'btn btn-danger',
                        buttonsStyling: false,
                    })
                    swalWithBootstrapButtons.fire({
                        title: 'Are you sure?',
                        text: "You won't be able to revert this!",
                        type: 'warning',
                        showCancelButton: true,
                        confirmButtonText: 'Yes, delete it!',
                        cancelButtonText: 'No, cancel!',
                        reverseButtons: true
                    }).then((result) => {
                        if (result.value) {
                            debugger;
                            var Code = $("#Code").val();
                            var currdate = $("[name*='DateINMonth']").val();
                                $.ajax({
                                    async: false,
                                    cache: false,
                                    type: "POST",
                                    data: "{SalCode:'" + Code + "',currdate:'" + currdate + "'}",//ids:'" + ids + "',
                                    url: "/SalaryProcess/DeleteSalaryProcess",
                                    dataType: 'Json',
                                    contentType: "Application/json",
                                    success: function (data) {                                     
                                        if (data=true) {
                                            debugger;
                                            RefreshDatatable();
                                            $("#btnCancel").trigger("click");
                                        } else {
                                            EzAlertdele(Code);
                                        }
                                    }
                                });

                            swalWithBootstrapButtons.fire(
                              'Deleted!',
                              'Your Code has been deleted.',
                              'success'
                            )

                        } else if (
                            // Read more about handling dismissals
                          result.dismiss === Swal.DismissReason.cancel
                        ) {
                            swalWithBootstrapButtons.fire(
                              'Cancelled',
                              'Your Code is safe :)',
                              'error'
                            )
                        }
                    })
                }
        });
        $("#btnSave").click(function () {

            var n = EzAuthenticationBtn("/SalaryProcess", "EditIt");
            
            $("#editshow").hide();
           
            var opMode = $("#hdnOperationMode").val();
            var currdate = $("[name*='DateINMonth']").val();
            if (opMode == "Add") {
                // Code for Adding MainTable
                var salarym = {
                    EditFlag: false,
                    //PRSM001UID :$("[name*='PRSM001UID']").val(),
                    Code: $("[name*='Code']").val(),
                    CurrentDate: $("[name*='DateINMonth']").val(),
                
                    salaryList: [],

                };
                $("#tableslryDetails tbody tr").each(function (index, item) {
                
                    var SnoTxt = $(this).find('td:eq(0)').text();
                    var EmpCodeTxt = $(this).find('td:eq(1)').text();
                    var EmpNameTxt = $(this).find('td:eq(2)').text();
                    var ProjectCodeTxt = $(this).find('td:eq(3)').text();
                    var WorkingDay = $(this).find('td:eq(4)').text();
                    var Present = $(this).find('td:eq(5)').text();
                    var Absent = $(this).find('td:eq(6)').text();
                    var Leaves = $(this).find('td:eq(7)').text();
                    var SickLeaves = $(this).find('td:eq(8)').text();
                    var WeeklyOff = $(this).find('td:eq(9)').text();
                    var Holiday = $(this).find('td:eq(10)').text();
                    var NormalOTHrs = $(this).find('td:eq(11)').text();
                    var HolidayOTHrs = $(this).find('td:eq(12)').text();
                    var WeeklyOffOTHrs = $(this).find('td:eq(13)').text();
                    var ExtraOTHrs = $(this).find('td:eq(14)').text();
                    var BasicSalary = $(this).find('td:eq(15)').text();
                    var MonthlyAddition = $(this).find('td:eq(16)').text();
                    var TotalDeduction  = $(this).find('td:eq(17)').text();
                    var LoanDeduction = $(this).find('td:eq(18)').text();
                    var TotalEarning = $(this).find('td:eq(19)').text();
                    var NetSalary = $(this).find('td:eq(20)').text();
                    var FExtraOTHrs = 0;

                    if ((EmpCodeTxt != "")) {
                        var salarydetail = {
                            Sno: SnoTxt,
                            EmpCode: EmpCodeTxt,
                            EmpName: EmpNameTxt,
                            ProjectCode:ProjectCodeTxt,
                            WorkingDay: WorkingDay,
                            Present: Present,
                            Absent: Absent,
                            Leaves: Leaves,
                            SickLeaves: SickLeaves,
                            WeeklyOff: WeeklyOff,
                            Holiday: Holiday,
                            NormalOTHrs: NormalOTHrs,
                            HolidayOTHrs: HolidayOTHrs,
                            WeeklyOffOTHrs: WeeklyOffOTHrs,
                            ExtraOTHrs: ExtraOTHrs,
                            FExtraOTHrs: FExtraOTHrs,
                            MonthlyAddition: MonthlyAddition,
                            TotalEarning: TotalEarning,
                            TotalDeduction: TotalDeduction,
                            LoanDeduction: LoanDeduction,
                            NetSalary: NetSalary
                        };
                        salarym.salaryList.push(salarydetail);
                     }
                });
                var validationResult = ValidateForm(salarym);
                if (validationResult.formValid) {
                    $.post("/SaveSalaryProcess", salarym).done(function (response) {
                       
                        if (response.SaveFlag && !response.ErrorMessage.length > 0) {
                          
                            EzbtnsaveAc("#SMAContainer");
                            ezerrormsg("#ErrorMessage", response.ErrorMessage);
                            GetSry();
                        } else {
                            Ezerrormsg("#ErrorMessage",response.ErrorMessage);
                        }
                    });

                } else {
                    $("#ErrorMessage").show();
                    Ezerrormsg("#ErrorMessage",validationResult.ErrorMessage);
                }
            }
            else if (opMode == "Edit" && n == 1) {
                    // Code for Adding Purchase order
                //
                var i = 0;
                $("#btnDelete").prop("disabled", true);
                $("#btnSave").prop("disabled", true);
                EzAlerterrtxt("Can't Edit Data");
                if (i = 1) {
                    var salarym = {
                        EditFlag: true,
                        //PRSM001UID :$("[name*='PRSM001UID']").val(),
                        PRSM001_CODE: $("[name*='Code']").val(),
                        CurrentDate: $("[name*='CurrentDate']").val(),

                        salaryList: []

                    };
                    $("#tableslryDetails tbody tr").each(function (index, item) {

                        var SnoTxt = $(this).find("[name*='Sno']").val();
                        var EmpCodeTxt = $(this).find("[name*='EmpCode']").val();
                        var EmpNameTxt = $(this).find("[name*='EmpName']").val();
                        var WorkingDay = $(this).find("[name*='WorkingDay']").val();
                        var Present = $(this).find("[name*='Present']").val();
                        var Absent = $(this).find("[name*='Absent']").val();
                        var Leaves = $(this).find("[name*='Leaves']").val();
                        var SickLeaves = $(this).find("[name*='SickLeaves']").val();
                        var WeeklyOff = $(this).find("[name*='WeeklyOff']").val();
                        var Holiday = $(this).find("[name*='Holiday']").val();
                        var NormalOTHrs = $(this).find("[name*='NormalOTHrs']").val();
                        var HolidayOTHrs = $(this).find("[name*='HolidayOTHrs']").val();
                        var WeeklyOffOTHrs = $(this).find("[name*='WeeklyOffOTHrs']").val();
                        var ExtraOTHrs = $(this).find("[name*='ExtraOTHrs']").val();
                        var FExtraOTHrs = $(this).find("[name*='FExtraOTHrs']").val();
                        var MonthlyAddition = $(this).find("[name*='MonthlyAddition']").val();
                        var TotalEarning = $(this).find("[name*='TotalEarning']").val();
                        var TotalDeduction = $(this).find("[name*='TotalDeduction']").val();
                        var LoanDeduction = $(this).find("[name*='LoanDeduction']").val();
                        var NetSalary = $(this).find("[name*='NetSalary']").val();

                        if ((EmpCodeTxt != "")) {
                            var salarydetail = {
                                Sno: SnoTxt,
                                EmpCode: EmpCodeTxt,
                                EmpName: EmpNameTxt,
                                WorkingDay: WorkingDay,
                                Present: Present,
                                Absent: Absent,
                                Leaves: Leaves,
                                SickLeaves: SickLeaves,
                                WeeklyOff: WeeklyOff,
                                Holiday: Holiday,
                                NormalOTHrs: NormalOTHrs,
                                HolidayOTHrs: HolidayOTHrs,
                                WeeklyOffOTHrs: WeeklyOffOTHrs,
                                ExtraOTHrs: ExtraOTHrs,
                                FExtraOTHrs: FExtraOTHrs,
                                MonthlyAddition: MonthlyAddition,
                                TotalEarning: TotalEarning,
                                TotalDeduction: TotalDeduction,
                                LoanDeduction: LoanDeduction,
                                NetSalary: NetSalary
                            };
                            salarym.salaryList.push(salarydetail);
                        }
                    });
                    var validationResult = ValidateForm(salarydetail);
                    if (validationResult.formValid) {
                        $.post("/SaveSalaryProcess", salarym).done(function (response) {
                            if (response.SaveFlag && !response.ErrorMessage.length > 0) {
                                EzbtnsaveAc("#SMAContainer");
                                ezerrormsg("#ErrorMessage", response.ErrorMessage);
                                GetSry();
                            } else {
                                ezerrormsg("#ErrorMessage", response.ErrorMessage);
                            }
                        });
                    } else {
                        $("#ErrorMessage").show();
                        ezerrormsg("#ErrorMessage", validationResult.ErrorMessage);
                    }
                }
              
            }


        });

        function ValidateForm(salarym) {
          
            var response = {
                ErrorMessage: "",
                formValid: false
            };

            if (!salarym.CurrentDate) {
                response.ErrorMessage += "CurrentDate";
            }
            //if (!salarym.Effect_From) {
            //    response.ErrorMessage += "Effect_From,";
            //}

            if (!salarym.Code) {
                response.ErrorMessage += "Code are mandatory Fields.";
            }



                //if (salarym.salarydetail.length == 0) {
                //        response.ErrorMessage += "Please Enter atleast One Item code Details";
                //    }
                    if (response.ErrorMessage.length == 0) {
                        response.formValid = true;
                    }

                    return response;
                }
        function RefreshDatatable()
        {
            var Code = $("#Code").val();

            $.get("/SalaryProcess/GetSalaryProcessGridEdit", { salP_code: Code }).done(function (responce) {
                    GetslryDetailsDatatable(responce);
              
            });
        }
        function GetslryDetailsDatatable(responce) {
            $("#divsrly").show();
            jq.fn.dataTable.ext.errorMode = 'none';
            var t = jq('#srlyprocssdatatabl').DataTable({
                "ColumnDefs": [{ "targets": [0], "Visible": false, "searchable": false }],
                data: responce,
                "order": [[1, "desc"]],
                "scrollX": true,
                "destroy": true,
                "columns": [
                     { "data": null },
                    //{"data":null,
                    //"render": function (data) {
                    //    return "<label class='containerone'>" + "<input type='checkbox' class='chck' value='" + data.EmpCode + "'>" + "<span class='chekmark></span>" + "</label>";
                    //}
                   // },

                           { "data": "EmpCode" },
                           { "data": "EmpName" },
                           { "data": "ProjectCode" },
                           { "data": "WorkingDay" },
                           { "data": "Present" },
                           { "data": "Leaves" },
                           { "data": "Absent" },
                           { "data": "SickLeaves" },
                           { "data": "WeeklyOff" },
                           { "data": "Holiday" },
                           { "data": "NormalOTHrs" },
                           { "data": "HolidayOTHrs" },
                           { "data": "WeeklyOffOTHrs" },
                           { "data": "ExtraOTHrs" },
                           { "data": "Salary" },
                           { "data": "MonthlyAddition" },
                           { "data": "TotalDeduction" },
                           { "data": "LoanDeduction" },
                           { "data": "TotalEarning" },
                           { "data": "NetSalary" },

                ]
            });
            t.on('order.dt search.dt', function () {
                t.column(0, { search: 'applied', order: 'applied' }).nodes().each(function (cell, i) {
                    cell.innerHTML = i + 1;
                });
            }).draw();
        }

         $(document).on("click", "#SalaryList tbody tr", function ()
                {

                    var operationMode = $("#hdnOperationMode").val();
                    if (operationMode == "Edit")
                    {
                       //
                        $("#SalaryList > tbody").children().removeClass("active");
                        $(this).addClass("active");
                        var Code = $(this).find("#hdnprbmCode").val();
                        $.get("EditSalaryProcess", { salP_code: Code }).done(function (response)
                        {
                            $("#SMAContainer").html(response);
                            $("#btnDelete").prop("disabled", false);
                         
                        });
                    }
                });
               
    });
    $(".datepicker").on("dp.show", function (e) {
        $(e.target).data("DateTimePicker").viewMode("months");
    });
</script>
<style>
    .top-buffer {
        margin-top: 15px;
    }

    .fixed-width {
        width: 100px;
        float: left;
    }

    .fluid.row-fluid {
        margin-right: 100px;
        -webkit-box-sizing: border-box;
        -moz-box-sizing: border-box;
        box-sizing: border-box;
    }

    .customSpace {
        content: " ";
        display: inline-block;
        width: 40px;
    }

    div.scroll {
        width: 300px;
        height: 500px;
        overflow: scroll;
    }
</style>

<div class="row">
    <div class="col-md-6 col-sm-6 col-xs-6" style="margin-top: 15px">
        <ol class="breadcrumb">
            <li class="active strong1">
                <strong>Salary Process</strong>
            </li>
        </ol>
        <div class="title-action">
            <button class="btn btn-primary btn1" id="btnNew"><span class="glyphicon glyphicon-plus-sign"></span>&nbsp;New</button>

            <button type="submit" class="btn btn-success btn1" id="btnEdit" data-toggle="modal" data-target="#editshow" data-keyboard="true"><span class="glyphicon glyphicon-edit"></span>&nbsp; Edit</button>

            <button type="submit" class="btn btn-primary btn1" id="btnSave" disabled><span class="glyphicon glyphicon-floppy-disk"></span>&nbsp; Save</button>

            <button class="btn btn-danger btn1" id="btnDelete" disabled><span class="glyphicon glyphicon-remove"></span>&nbsp; Delete</button>

            <button class="btn btn-warning btn1" id="btnCancel"><span class="glyphicon glyphicon-off"></span>&nbsp; Cancel</button>

            <input type="hidden" id="hdnOperationMode" value="" />

        </div>
    </div>

</div><hr />
<!-- Row end -->
<!-- Row start -->
<div class="row">
    <!--updated code-->

    <div class="col-md-12 no-padding navbar-expand-lg">
        <div class="panel panel-primary">
            <div class="panel-heading clearfix">
                <i class="icon-calendar"></i>
                <h3 class="panel-title">Ez Business System</h3>
            </div>
            <div class="panel-body" style="margin-left:10px;">
                <div class="tabbable-line boxless tabbable-reversed" id="SMAContainer">
                    <label class="row" id="ErrorMessage" style="color:red">
                        <strong></strong>
                    </label>
                    <div class="custom-form-wrapper">
                        <div id="SMAContainer">
                            <div class="row">
                                <div class="custom-form-wrapper">
                                    <!-- BEGIN FORM-->
                                    <div class="form-horizontal">
                                        <div class="form-body">
                                            <div class="row">
                                                <div class="col-md-6">
                                                    <div class="form-group">
                                                        <label class="control-label col-md-3">CODE</label>
                                                        <div class="col-md-8 input-field">
                                                            <input type="text" class="form-control" name="" required placeholder="" maxlength="50" disabled>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <!--/row-->
                                            <div class="row">
                                                <div class="col-md-6">
                                                    <div class="form-group">
                                                        <label class="control-label col-md-3">Date</label>
                                                        <div class="col-md-8 input-field">
                                                            <input id="DateINMonth" class="form-control datepicker" name="DateINMonth">
                                                        </div>
                                                    </div>
                                                </div>
                                                <!--/span-->
                                                <div class="col-md-6">
                                                    <button class="btn btn-warning"   type="submit" style="width:10em">Calculate</button>
                                                </div>
                                                <!--/span-->
                                            </div>
                                        </div>
                                    </div><br />
                                    <!-- END FORM-->
                                    <div class="">
                                        <ul class="nav nav-tabs">
                                            <li class="active"><a data-toggle="tab" href="#home">Detail</a></li>

                                        </ul>
                                        <div class="tab-content" style="display:none" id="divsrly">
                                            <div id="home" class="tab-pane fade in active">
                                                <br /><br />
                                                <div class="row">
                                                    <div class="col-md-12">
                                                        <div style="overflow-x:auto;">
                                                            <table class="table table-bordered" id="srlyprocssdatatabl">
                                                                <thead>
                                                                    <tr>
                                                                        <th>Sr No</th>
                                                                        @*<th><label class="containerone" style="padding-left:35px"><input type="checkbox" class="chck chckheaderdata"><span class="chekmark"></span></label></th>*@

                                                                        <th>Emp_Code</th>
                                                                        <th>Employee Name</th>
                                                                        <th>ProjectCode</th>
                                                                        <th>Working Days</th>
                                                                        <th>Present</th>
                                                                        <th>Absent</th>
                                                                        <th>Leaves</th>
                                                                        <th>Sick Leaves</th>
                                                                        <th>Weekly Off</th>
                                                                        <th>Holiday</th>
                                                                        <th>NOT Hrs</th>
                                                                        <th>HOT Hrs</th>
                                                                        <th>WOT hrs</th>
                                                                        <th>Extra OT Hrs</th>
                                                                        <th>Basic Salary</th>
                                                                        <th>Monthly Addition</th>
                                                                        <th>Monthly Deduction</th>
                                                                        <th>Loan Deduction</th>
                                                                        <th>Total Earning</th>
                                                                        <th>Net Salary</th>
                                                                    </tr>
                                                                </thead>
                                                                <tbody></tbody>
                                                            </table>
                                                        </div>
                                                    </div>

                                                </div>
                                            </div>

                                        </div>
                                    </div>

                                    <!-- <button>Hey, click me!</button> -->
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <div class="modal" id="editshow" role="dialog">
            <div class="modal-dialog m1" data-keyboard="false" style="width: 350px;height: auto;margin-left:800px;margin-top:50px;">
                <div class="modal-content">


                    <div class="modal-header">
                        <h4 class="modal-title">Ez Business System</h4>
                        <button type="button" style="float: right;margin-top: -26px;" class="btn btn-danger alert-close" data-dismiss="modal" aria-hidden="true"><i class="fa fa-close"></i></button>

                    </div>


                    <div class="modal-body">
                        <div class="container scroll" id="SMAListContainer">
                            @Html.Action("GetSalaryDetails", "SalaryProcess")
                        </div>
                    </div>

                </div>
            </div>
        </div>
    <!-- Purchase Request List End -->
    <!-- Row end -->
</div>


